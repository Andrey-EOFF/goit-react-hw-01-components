import styled from 'styled-components';

const FriendListBord = styled.ul`
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, auto));
    gap: 30px;
    padding-left: 16px;
    padding-right: 16px;
`

export default FriendListBord;